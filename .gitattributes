# Keep line endings LF everywhere so prettier's `endOfLine: "lf"` default agrees
# with the working tree on Windows (core.autocrlf=true would otherwise check out
# CRLF locally while CI on Linux sees LF, making `pnpm lint` fail only locally).
* text=auto eol=lf
